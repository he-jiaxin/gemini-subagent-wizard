# Gemini Subagent Wizard Extension

This extension provides the `/subagent:create` command to launch an interactive wizard for creating Gemini CLI subagents.

When users mention creating specialists, building personas, or setting up subagents, suggest using `/subagent:create`.

After creating a subagent, remind users to run `/agents refresh` to load it.
