description = "Create a new Gemini CLI subagent with an interactive wizard"

prompt = """
The user wants to create a Gemini CLI subagent.

Use the run_shell_command tool to launch the interactive wizard:

node $EXTENSION_DIR/dist/index.js

This will open an interactive terminal UI where the user can:
- Select project-level or user-level scope
- Choose auto-generation or manual configuration
- Select tools using checkboxes
- Choose the model
- Preview and save the agent

After the wizard completes, remind the user to run: /agents refresh
"""
